apiVersion: v1
kind: Service
metadata:
  name: simple-time-service
spec:
  selector:
    app: simple-time-service
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
